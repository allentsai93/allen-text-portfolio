import React from 'react';
import styled from 'styled-components';

const StyledContainer = styled.div`
    width: 100%;
    display: flex;
    flex-flow: row wrap;
    justify-content: flex-start;
    align-items: center;
    flex: 1;
    background-color: #38ffd1d6;
`;

const InputSection = () => {
    return (
        <StyledContainer>

        </StyledContainer>
    )
};

export default InputSection;